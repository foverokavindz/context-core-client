import { styled } from '@mui/material/styles';
import Box from '@mui/material/Box';

export const StyledChatPanelRoot = styled(Box)({
	display: 'flex',
	flex: 1,
	minHeight: 0,
	overflow: 'hidden',
});
